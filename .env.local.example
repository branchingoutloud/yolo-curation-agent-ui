# Copy to .env.local to skip the Settings dialog's LangSmith API key field.
# Deployment URL and Assistant ID are NOT set here - configure those via the
# Settings UI in the app (persisted to localStorage), per README.md.
NEXT_PUBLIC_LANGSMITH_API_KEY=
